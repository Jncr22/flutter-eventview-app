import 'package:flutter/material.dart';

class NewsViewStudent extends StatelessWidget {
  const NewsViewStudent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: const Text('noticias student'),);
  }
}