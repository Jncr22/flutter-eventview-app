import 'package:flutter/material.dart';

class NewsNotice extends StatelessWidget {
  const NewsNotice({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Noticias"),
    );
  }
}
